import { createDeviceLectureSpeech } from "../lib/lecture-device-speech.ts";
import { attachLectureGestures } from "../lib/lecture-player-gestures.ts";
import { lectureTranscriptSegments } from "../lib/lecture-transcript.ts";
export const lecturePlayerUri="ui://interview-arc/lecture-player-v3.html";
export const lecturePlayerHtml=String.raw`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="referrer" content="no-referrer"><title>Professor lecture</title><style>

:root{color-scheme:light dark;--paper:#fbfcfa;--ink:#102a2a;--muted:#526962;--line:#c8d8d3;--accent:#0f5f5a;--signal:#b9db57;--well:#edf3f1}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.5 system-ui,sans-serif}main{padding:24px;max-width:660px;margin:auto}h1{font:600 24px/1.2 Georgia,serif;letter-spacing:-.02em;margin:0 0 8px;overflow-wrap:anywhere}p{margin:8px 0}small,.hint{font-size:12px;color:var(--muted)}button,select,summary{font:inherit;min-height:44px}button,select{border:1px solid var(--line);border-radius:8px;padding:8px 12px;background:var(--paper);color:var(--ink)}button{cursor:pointer}button:disabled{opacity:.5;cursor:default}button:focus-visible,select:focus-visible,summary:focus-visible{outline:3px solid var(--accent);outline-offset:3px}#touchPlayer{width:100%;height:128px;border:0;border-radius:14px;background:var(--well);display:flex;align-items:center;justify-content:space-around;margin:22px 0 14px;touch-action:pan-y;user-select:none;-webkit-user-select:none;-webkit-touch-callout:none}#touchPlayer .center{display:grid;place-items:center;width:76px;height:76px;border-radius:50%;background:var(--accent);color:#fff}svg{width:28px;height:28px;stroke:currentColor;stroke-width:1.8;fill:none;stroke-linecap:round;stroke-linejoin:round}#playIcon{fill:currentColor;stroke:none}#touchPlayer .side{color:var(--muted);font-size:14px}#feedback{min-height:22px;text-align:center;color:var(--accent);font-size:13px}#timeline{width:100%;height:5px;border:0;border-radius:4px;display:block;accent-color:var(--accent);overflow:hidden}progress::-webkit-progress-bar{background:var(--line)}progress::-webkit-progress-value{background:var(--accent)}.position{display:flex;justify-content:space-between;gap:12px;font-size:12px;color:var(--muted);margin-top:8px}.transport{display:flex;align-items:center;justify-content:center;gap:14px;margin:16px 0}.transport button{min-width:60px}.transport select{min-width:80px}.hint{text-align:center;margin:0 0 20px}details{border-top:1px solid var(--line);padding-top:8px;margin-top:8px}summary{cursor:pointer;display:flex;align-items:center;justify-content:space-between;font-size:14px}summary:after{content:'+';font-size:20px}details[open] summary:after{content:'−'}label{display:grid;gap:6px;margin:12px 0}select{width:100%;max-width:100%;text-overflow:ellipsis}#chapters{display:grid;gap:6px;margin:10px 0}#chapters button{text-align:left;border:0;background:var(--well)}#chapters button[aria-current=true]{box-shadow:inset 3px 0 var(--accent)}#error{color:#b63823;font-size:14px;white-space:pre-wrap}#deviceState{font-size:14px;color:var(--muted)}audio,#legacy{display:none}.transport label{margin:0}#feedback:empty,#deviceState:empty,#error:empty{display:none}@media(prefers-color-scheme:dark){:root{--paper:#122524;--ink:#e8f2ee;--muted:#b1c6bf;--line:#3d5750;--accent:#b9db57;--well:#1d3531}#touchPlayer .center{color:#142b26}#error{color:#ffb5a5}}@media(max-width:480px){main{padding:20px}h1{font-size:23px}#touchPlayer{height:112px}}
.transcript-panel{border-top:1px solid var(--line);padding:16px 0 8px}.transcript-heading{display:flex;align-items:center;justify-content:space-between;gap:10px}.transcript-heading button{font-size:12px;border:0;background:transparent}.transcript-heading strong{font-size:15px}#transcript{max-height:240px;overflow:auto;overscroll-behavior:contain;position:relative;margin:12px -8px;padding:0 8px;scrollbar-width:thin}#transcript button{display:block;width:100%;border:0;text-align:left;white-space:pre-wrap;padding:10px 12px;font-size:16px;line-height:1.65;color:var(--muted);background:transparent;margin:0}#transcript button[aria-current=true]{background:var(--well);color:var(--ink);font-weight:600}#transcript button:hover{color:var(--ink)}#transcript:focus-visible{outline:2px solid var(--accent)}[hidden]{display:none!important}</style></head><body><main><h1 id="title">Your lecture</h1><p id="details" class="hint" style="text-align:left;margin:0">Opening lesson…</p>
<button id="touchPlayer" aria-label="Play lecture" aria-describedby="gestureHelp" disabled><span class="side" aria-hidden="true">−5s</span><span class="center"><svg id="playIcon" viewBox="0 0 24 24" aria-hidden="true"><path d="M8 4v16l13-8z"/></svg><svg id="pauseIcon" viewBox="0 0 24 24" aria-hidden="true" hidden><path d="M8 5v14M16 5v14"/></svg></span><span class="side" aria-hidden="true">+5s</span></button>
<progress id="timeline" value="0" max="100" aria-label="Lecture progress"></progress><div class="position"><span id="chapterLabel">Ready</span><span id="elapsed">0%</span></div>
<div id="feedback" role="status" aria-live="polite"></div>
<div class="transport"><button id="back" aria-label="Back about five seconds">−5s</button><label><span hidden>Playback speed</span><select id="speed" aria-label="Playback speed"><option value="0.75">0.75×</option><option value="1" selected>1×</option><option value="1.25">1.25×</option><option value="1.5">1.5×</option><option value="2">2×</option></select></label><button id="forward" aria-label="Forward about five seconds">+5s</button></div>
<p class="hint" id="gestureHelp">Tap to play / pause · Double-tap sides to skip<br>Hold for 2× · Device-voice skips are approximate</p><p id="deviceState" role="status"></p><p id="error" role="alert"></p>
<section aria-label="Transcript" class="transcript-panel"><div class="transcript-heading"><strong>Transcript</strong><button id="follow" aria-pressed="true">Following</button></div><small>Tap a passage to play from there.</small><div id="transcript" tabindex="0" aria-label="Lecture transcript"></div><div class="transcript-heading"><button id="previousText" aria-label="Previous transcript section">Previous</button><small id="textPage"></small><button id="nextText" aria-label="Next transcript section">Next</button></div></section>
<details><summary>Voice &amp; playback</summary><label>Voice on this device<select id="voice" aria-label="Voice on this device"></select></label><small id="voiceNote">Only installed voices available to ChatGPT appear here. No speech API charges.</small><p><small>Keep the player open. Background and screen-lock playback depend on ChatGPT.</small></p><button id="refresh">Reconnect player</button><button id="hour" hidden disabled>Fit full lecture to 1 hour</button></details>
<details><summary>Chapters</summary><div id="chapters" aria-label="Lecture chapters"></div></details>
<audio id="audio" preload="metadata"></audio><div id="legacy" hidden><button id="devicePlay" disabled>Play free on this device</button><button id="devicePause" disabled>Pause</button><p id="state"></p><progress id="progress"></progress><button id="generate" hidden disabled>Generate paid API audio</button><button id="stop" hidden>Stop after this section</button><p id="duration"></p><small id="saved"></small></div>
</main><script>
// Wrangler adds function-name annotations when bundling the shared controller.
// They are metadata only; keep their helper local to this serialized script.
const __name=fn=>fn;
const createDeviceLectureSpeech=${createDeviceLectureSpeech.toString()};
const attachLectureGestures=${attachLectureGestures.toString()};
const lectureTranscriptSegments=${lectureTranscriptSegments.toString()};
const $=id=>document.getElementById(id),audio=$('audio'),pending=new Map();let nextId=1,lecture=null,generating=false,stop=false,saving=null,queued=null,lastSave=0,dirty=false,blocked=false,starts=[],latestPosition=0,canGenerate=false;
let device=null,devicePhase='paused',deviceSave=null,recording=false,characterStarts=[0];
function updateTransport(phase,index=0,offset=0){const active=['playing','loading'].includes(phase);$('touchPlayer').setAttribute('aria-label',active?'Pause lecture':phase==='finished'?'Replay lecture':'Play lecture');$('playIcon').style.display=active?'none':'block';$('pauseIcon').style.display=active?'block':'none';$('chapterLabel').textContent=lecture?.chunks[index]?.sectionTitle||'Ready';if(!recording&&lecture){const total=characterStarts[characterStarts.length-1],before=characterStarts[index]||0;const progress=total?Math.min(100,100*(before+offset)/total):0;$('timeline').value=progress;$('elapsed').textContent=Math.round(progress)+'%';}if(['error','unavailable'].includes(phase))$('touchPlayer').disabled=true;}
function toggle(){if(recording){if(audio.paused)audio.play().catch(error);else audio.pause();}else if(['playing','loading'].includes(devicePhase))void device?.pause().catch(error);else void device?.play().catch(error);}
function skip(seconds){if(recording&&Number.isFinite(audio.duration)){audio.currentTime=Math.max(0,Math.min(audio.duration,audio.currentTime+seconds));dirty=true;void save().catch(()=>{});}else void device?.skip(seconds).catch(error);$('feedback').textContent=(recording?'':'About ')+(seconds<0?'−5s':'+5s');}
attachLectureGestures($('touchPlayer'),{toggle,skip,boost:active=>{const rate=active?2:Number($('speed').value);audio.playbackRate=rate;device?.setRate(rate);$('feedback').textContent=active?'2× while holding':'';}});$('back').onclick=()=>skip(-5);$('forward').onclick=()=>skip(5);$('voice').onchange=()=>device?.setVoice($('voice').value);

let textIndex=-1,textEpoch=0,followText=true,currentTextState={chunkIndex:0,characterOffset:0},fragmentCache=new Map(),textRows=[],activeTextRow=null;
async function loadText(index){if(!fragmentCache.has(index)){const id=lecture.lectureId,fingerprint=lecture.fingerprint;const promise=call('get_practice_lecture',{lectureId:id,chunkIndex:index}).then(result=>{const d=result.structuredContent;if(d?.fingerprint!==fingerprint||d?.fragment?.index!==index)throw Error('Transcript could not be confirmed. Reconnect to retry.');return d.fragment.text;});fragmentCache.set(index,promise);promise.catch(()=>fragmentCache.delete(index));}return fragmentCache.get(index);}
function highlightText(){if(textIndex!==currentTextState.chunkIndex)return;const offset=currentTextState.characterOffset;let low=0,high=textRows.length;while(low<high){const mid=(low+high)>>>1;if(Number(textRows[mid].dataset.start)<=offset)low=mid+1;else high=mid;}const candidate=textRows[low-1],active=candidate&&Number(candidate.dataset.end)>offset?candidate:null;if(active!==activeTextRow){activeTextRow?.setAttribute('aria-current','false');active?.setAttribute('aria-current','true');activeTextRow=active;}if(active&&followText){const box=$('transcript');if(active.offsetTop<box.scrollTop||active.offsetTop+active.offsetHeight>box.scrollTop+box.clientHeight)box.scrollTop=Math.max(0,active.offsetTop-box.clientHeight/3);}}
async function showText(index){if(!lecture||index<0||index>=lecture.chunks.length)return;const token=++textEpoch;textIndex=index;textRows=[];activeTextRow=null;$('textPage').textContent=(index+1)+' / '+lecture.chunks.length;$('previousText').disabled=index===0;$('nextText').disabled=index===lecture.chunks.length-1;$('transcript').textContent='Loading transcript…';try{const text=await loadText(index);if(token!==textEpoch)return;$('transcript').replaceChildren();for(const part of lectureTranscriptSegments(text)){const row=document.createElement('button');row.textContent=part.text;row.dataset.start=part.start;row.dataset.end=part.end;row.onclick=()=>{setFollow(true);if(recording){audio.currentTime=(starts[index]||0)+(lecture.chunks[index].audio?.duration_seconds||0)*part.start/Math.max(1,text.length);dirty=true;audio.play().catch(error);$('feedback').textContent='Recording position is approximate.';}else void device?.seek(index,part.start,true).catch(error);};row.setAttribute('aria-current','false');textRows.push(row);$('transcript').appendChild(row);}highlightText();}catch(e){if(token===textEpoch)$('transcript').textContent=e.message;}}
function setFollow(value){followText=value;$('follow').setAttribute('aria-pressed',String(value));$('follow').textContent=value?'Following':'Follow playback';if(value){if(textIndex!==currentTextState.chunkIndex)void showText(currentTextState.chunkIndex);else highlightText();}}
$('follow').onclick=()=>setFollow(!followText);$('transcript').addEventListener('wheel',()=>setFollow(false),{passive:true});$('transcript').addEventListener('touchmove',()=>setFollow(false),{passive:true});$('transcript').addEventListener('keydown',event=>{if(['ArrowUp','ArrowDown','PageUp','PageDown','Home','End'].includes(event.key))setFollow(false);});$('previousText').onclick=()=>{setFollow(false);void showText(textIndex-1);};$('nextText').onclick=()=>{setFollow(false);void showText(textIndex+1);};
let voiceSignature='';function deviceVoices(){const available=device?.voices()??[],selected=$('voice').value,signature=available.map(v=>v.voiceURI).join('|');if(signature!==voiceSignature){voiceSignature=signature;$('voice').replaceChildren();for(const voice of available){const option=document.createElement('option');option.value=voice.voiceURI;option.textContent=voice.name+' · '+voice.lang;$('voice').appendChild(option);}if(available.some(v=>v.voiceURI===selected))$('voice').value=selected;else{const preferred=available.find(v=>/premium|enhanced|natural/i.test(v.name))??available.find(v=>v.default)??available[0];if(preferred)$('voice').value=preferred.voiceURI;}}$('touchPlayer').disabled=!recording&&(!device||!available.length||devicePhase==='error');if(!available.length&&!recording)$('deviceState').textContent='No installed English voice is available here. Try another device.';}
function setupDevice(fragment){device?.dispose();device=null;devicePhase='paused';fragmentCache=new Map();if(fragment)fragmentCache.set(fragment.index,Promise.resolve(fragment.text));currentTextState={chunkIndex:lecture.cursor.chunkIndex,characterOffset:lecture.cursor.characterOffset};void showText(lecture.cursor.chunkIndex);if(!window.speechSynthesis||!window.SpeechSynthesisUtterance){$('deviceState').textContent='This host does not expose device speech. No paid request was made.';return;}
const id=lecture.lectureId;device=createDeviceLectureSpeech({speech:window.speechSynthesis,makeUtterance:text=>new SpeechSynthesisUtterance(text),chunkCount:lecture.chunks.length,chunkIndex:lecture.cursor.chunkIndex,characterOffset:lecture.cursor.characterOffset,
loadChunk:loadText,
savePosition:async(index,offset)=>{deviceSave??={lectureId:id,operationId:'device-'+crypto.randomUUID(),expectedRevision:lecture.cursor.revision,chunkIndex:index,characterOffset:offset,offsetSeconds:0};const result=await call('save_lecture_position',deviceSave),saved=result.structuredContent;if(!saved?.revision)throw Error('Position save was not confirmed. Reload before resuming.');lecture.cursor.revision=saved.revision;deviceSave=null;$('saved').textContent='Saved section '+(index+1)+' · character '+offset+' · revision '+saved.revision;},
onChange:state=>{devicePhase=state.phase;currentTextState=state;if(followText&&textIndex!==state.chunkIndex)void showText(state.chunkIndex);else highlightText();$('deviceState').textContent=['error','unavailable'].includes(state.phase)?state.message:state.phase==='finished'?'Finished. Tap to listen again.':'';updateTransport(state.phase,state.chunkIndex,state.characterOffset);$('devicePause').disabled=!['playing','loading'].includes(state.phase);deviceVoices();}});device.setRate(Number($('speed').value));$('deviceState').textContent='';deviceVoices();device.setVoice($('voice').value);updateTransport('paused',lecture.cursor.chunkIndex,lecture.cursor.characterOffset);}
window.speechSynthesis?.addEventListener('voiceschanged',deviceVoices);
$('devicePlay').onclick=async()=>{audio.pause();if(saving)await saving.catch(()=>{});if(blocked)return;void device?.play().catch(error);};$('devicePause').onclick=()=>device?.pause().catch(error);
window.addEventListener('pagehide',()=>{void device?.pause().catch(()=>{});device?.dispose();});
function notify(method,params){window.parent.postMessage({jsonrpc:'2.0',method,params},'*');}
function request(method,params){const id=nextId++;return new Promise((resolve,reject)=>{const timeout=setTimeout(()=>{pending.delete(id);reject(Error('Chat connection timed out. Reconnect and check saved state before retrying.'));},180000);pending.set(id,{resolve,reject,timeout});window.parent.postMessage({jsonrpc:'2.0',id,method,params},'*');});}
async function call(name,args){const result=await request('tools/call',{name,arguments:args});if(result?.isError)throw Error(result.content?.find(c=>c.type==='text')?.text||'Tool failed.');return result;}
function error(e){$('error').textContent=e.message||String(e);}
function time(n){return Math.floor(n/60)+':'+String(Math.floor(n%60)).padStart(2,'0');}
function duration(){if(!Number.isFinite(audio.duration))return;$('duration').textContent='Recording '+time(audio.duration)+' · listening time '+time(audio.duration/audio.playbackRate)+' at '+audio.playbackRate.toFixed(2)+'×';$('hour').disabled=audio.duration<1800||audio.duration>7200;}
function render(result){const data=result.structuredContent??result; if(!data.lecture)return;dirty=false;queued=null;lecture=data.lecture;blocked=false;$('title').textContent=lecture.title;$('details').textContent=lecture.chunks.length+' '+(lecture.chunks.length===1?'section':'sections')+' · '+(lecture.estimatedMinutes<1?'Under a minute':lecture.estimatedMinutes+' min estimated');recording=Boolean(data.ready);$('gestureHelp').innerText=recording?'Tap to play / pause · Double-tap sides to skip\nHold for 2× · Transcript timing is approximate':'Tap to play / pause · Double-tap sides to skip\nHold for 2× · Device-voice skips are approximate';$('back').setAttribute('aria-label',recording?'Back five seconds':'Back about five seconds');$('forward').setAttribute('aria-label',recording?'Forward five seconds':'Forward about five seconds');const count=lecture.chunks.filter(c=>c.audio?.state==='ready').length;$('progress').max=lecture.chunks.length;$('progress').value=count;$('state').textContent=data.ready?'Audio ready. Press Play.':count+' / '+lecture.chunks.length+' sections prepared';canGenerate=!data.ready&&data.speechConfigured;$('generate').disabled=generating||!canGenerate;if(!data.ready)$('state').textContent='Script ready for free playback.';
 starts=[];characterStarts=[0];let offset=0;for(const c of lecture.chunks){starts.push(offset);offset+=c.audio?.duration_seconds??0;characterStarts.push(characterStarts[characterStarts.length-1]+(c.characterCount||0));}
 const url=result._meta?.audioUrl;if(url){const parsed=new URL(url);if(parsed.protocol!=='https:'||parsed.origin!=='https://limitless-mcp.vinosama.workers.dev')throw Error('Unrecognized audio origin.');audio.src=url;latestPosition=(starts[lecture.cursor.chunkIndex]??0)+lecture.cursor.offsetSeconds;audio.onloadedmetadata=()=>{audio.currentTime=Math.min(latestPosition,audio.duration);duration();};}else{audio.removeAttribute('src');audio.load();}
 $('chapters').replaceChildren();let last='';for(const c of lecture.chunks){if(c.sectionId===last)continue;last=c.sectionId;const b=document.createElement('button');b.textContent=c.sectionTitle;b.setAttribute('aria-current',String(c.index===lecture.cursor.chunkIndex));b.onclick=()=>{if(data.ready){audio.currentTime=starts[c.index];dirty=true;}else void device?.seek(c.index).catch(error);};$('chapters').appendChild(b);} $('saved').textContent='Saved position revision '+lecture.cursor.revision;deviceSave=null;setupDevice(result._meta?.scriptFragment);audio.hidden=!data.ready;$('hour').hidden=!data.ready;$('progress').hidden=!data.ready;
}
async function refresh(){if(!lecture)return;await device?.pause();if(saving)await saving.catch(()=>{});if(dirty&&!blocked)await save();const result=await call('open_practice_lecture_player',{lectureId:lecture.lectureId});render(result);$('error').textContent='';}
async function save(){if(!lecture||blocked||!dirty)return;if(saving)return saving;saving=(async()=>{do{const position=audio.currentTime;let index=starts.findLastIndex(n=>n<=position);index=Math.max(0,index);const seconds=Math.min(Math.max(0,position-starts[index]),lecture.chunks[index].audio?.duration_seconds??0);queued??={lectureId:lecture.lectureId,operationId:'player-'+crypto.randomUUID(),expectedRevision:lecture.cursor.revision,chunkIndex:index,offsetSeconds:seconds,characterOffset:0};const result=await call('save_lecture_position',queued),saved=result.structuredContent;if(!saved?.revision)throw Error('Position save was not confirmed.');lecture.cursor.revision=saved.revision;queued=null;dirty=Math.abs(audio.currentTime-position)>.5;$('saved').textContent='Saved '+time(position)+' · revision '+saved.revision;}while(dirty&&!blocked);})().catch(e=>{blocked=true;audio.pause();error(e);$('saved').textContent='Save needs attention. Reconnect before resuming.';throw e;}).finally(()=>{saving=null;});return saving;}
audio.addEventListener('play',()=>{if(['playing','loading'].includes(devicePhase)){audio.pause();void device.pause().catch(error);return;}if(blocked){audio.pause();return;}dirty=true;});audio.addEventListener('timeupdate',()=>{if(audio.paused||blocked)return;dirty=true;if(Date.now()-lastSave>10000){lastSave=Date.now();save().catch(()=>{});}});audio.addEventListener('pause',()=>{if(dirty&&!blocked)save().catch(()=>{});});audio.addEventListener('ended',()=>{dirty=true;save().catch(()=>{});});audio.addEventListener('error',()=>{if(audio.getAttribute('src'))error(Error('Playback could not load. Refresh the player to renew its audio access.'));});
audio.addEventListener('play',()=>updateTransport('playing'));audio.addEventListener('pause',()=>updateTransport(audio.ended?'finished':'paused'));audio.addEventListener('timeupdate',()=>{if(!recording||!Number.isFinite(audio.duration))return;$('timeline').value=100*audio.currentTime/audio.duration;$('elapsed').textContent=time(audio.currentTime)+' / '+time(audio.duration);const index=Math.max(0,starts.findLastIndex(n=>n<=audio.currentTime)),chunk=lecture.chunks[index];currentTextState={chunkIndex:index,characterOffset:Math.floor((audio.currentTime-starts[index])/Math.max(.01,chunk.audio?.duration_seconds||1)*(chunk.characterCount||0))};if(followText&&textIndex!==index)void showText(index);else highlightText();$('chapterLabel').textContent=chunk.sectionTitle;});
$('speed').onchange=()=>{audio.playbackRate=Number($('speed').value);device?.setRate(audio.playbackRate);duration();};$('hour').onclick=()=>{if(audio.duration>=1800&&audio.duration<=7200){audio.playbackRate=audio.duration/3600;duration();}};
$('refresh').onclick=()=>refresh().catch(error);$('stop').onclick=()=>{stop=true;$('state').textContent='Finishing the current section, then stopping.';};
$('generate').onclick=async()=>{if(!lecture)return;generating=true;stop=false;$('generate').disabled=true;$('stop').hidden=false;$('error').textContent='';try{for(const c of lecture.chunks){if(stop)break;if(c.audio?.state==='ready')continue;$('state').textContent='Generating section '+(c.index+1)+' of '+lecture.chunks.length;await call('generate_lecture_audio_section',{lectureId:lecture.lectureId,chunkIndex:c.index});$('progress').value=Number($('progress').value)+1;}await refresh();}catch(e){error(e);}finally{generating=false;$('stop').hidden=true;$('generate').disabled=!canGenerate;}};
window.addEventListener('message',event=>{if(event.source!==window.parent)return;const m=event.data;if(!m||m.jsonrpc!=='2.0')return;const p=pending.get(m.id);if(p){pending.delete(m.id);clearTimeout(p.timeout);if(m.error)p.reject(Error(m.error.message));else p.resolve(m.result);return;}if(m.method==='ui/notifications/tool-result'&&!generating&&!saving&&!['playing','loading'].includes(devicePhase))try{render(m.params);}catch(e){error(e);}});
new ResizeObserver(()=>notify('ui/notifications/size-changed',{height:document.querySelector('main').getBoundingClientRect().height})).observe(document.querySelector('main'));
request('ui/initialize',{protocolVersion:'2026-01-26',appInfo:{name:'Interview Arc Professor',version:'1.1.0'},appCapabilities:{}}).then(()=>{notify('ui/notifications/initialized',{});if(window.openai?.toolOutput)render({structuredContent:window.openai.toolOutput,_meta:window.openai.toolResponseMetadata});}).catch(error);
</script></body></html>`;
